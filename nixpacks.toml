[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "chromium", "chromedriver", "ffmpeg"]

[phases.install]
cmds = ["python311 -m pip install -r requirements.txt"]

[start]
cmd = "python311 bot.py"

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "chromium", "chromedriver", "ffmpeg"]

[phases.install] 
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "python bot.py"

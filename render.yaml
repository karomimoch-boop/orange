services:
  - type: web
    name: orangecarrier-bot
    runtime: python
    buildCommand: "apt-get update && apt-get install -y chromium-driver ffmpeg && pip install -r requirements.txt"
    startCommand: "python bot.py"
    plan: free
    autoDeploy: true
